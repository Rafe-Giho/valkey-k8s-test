# Vendor

이 폴더는 외부에서 가져온 참고용 소스나 템플릿을 보관하는 영역입니다.

현재 포함된 항목:

- `valkey-helm`

`valkey-helm`은 공식 Valkey Helm chart 참고본입니다.

주의:
- 실제 배포 기준 문서는 `deployments` 폴더를 기준으로 봅니다.
- `vendor` 폴더는 원본 참고용이므로 직접 수정 대상보다는 비교/참고용에 가깝습니다.
